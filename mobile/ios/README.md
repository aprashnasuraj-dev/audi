# iOS Deep Dive Workflow

Target App Store ID: `1528802350`.

Implemented path:

- public App Store / AASA metadata analysis through `final_audit_runner.py`;
- universal-link and custom-scheme candidates are treated as routing hypotheses only;
- any iOS issue requires a researcher-controlled device/account and direct Minly impact.

No jailbreak-only dynamic testing is executed in CI.
